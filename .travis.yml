language: cpp
sudo: false
dist: trusty

addons:
  apt:
    packages:
      - gcovr

install:
  - ./autogen.sh
  - ./configure

script: 
  - make
  - make firmware_tool
  - make server
  - make assets
  - make run_server &
  - make check
